# Rubix default configuration.
# Copy to $XDG_CONFIG_HOME/rubix/config.toml (or ~/.config/rubix/config.toml) to override.

# Commands run once, via `sh -c`, after the compositor is up (Wayland socket
# published + XWayland ready). The Rubix equivalent of AwesomeWM's autorun.
# Here: import the compositor env into the systemd --user environment and start
# the Rubix session target, which pulls in the notification daemon (mako),
# clipboard (copyq), polkit agent, wallpaper (swaybg), and teams-for-notif.
startup = [
    "systemctl --user import-environment WAYLAND_DISPLAY DISPLAY XDG_CURRENT_DESKTOP && systemctl --user start rubix-session.target",
]

# SDR white luminance (nits) fed to the HDR encode shader for `hdr = true`
# outputs -- the "brightness slider" for SDR content composited through the
# HDR (BT.2020/PQ) pipeline. Live/hot-reloadable: editing and saving this
# value takes effect immediately, no restart. Also adjustable at runtime via
# Alt+XF86AudioRaiseVolume / Alt+XF86AudioLowerVolume (Super+ on a real TTY
# session -- see keybinds below). Valid range [80, 300]; out-of-range values
# are clamped. Omitting the key defaults to 203 (BT.2408 reference SDR white).
sdr_white_nits = 203.0

# Self-contained idle screen-off timer -- Rubix's own DPMS-equivalent, with no
# external daemon required. Powers every output off (real CRTC disable, not a
# black-filled framebuffer -- correct for OLED panels) after this many seconds
# of no keyboard/pointer activity, and back on on the next input. Held off
# while any client holds an idle-inhibit (idle-inhibit-unstable-v1) lock, e.g.
# a fullscreen video player or a Teams call.
# Live/hot-reloadable: editing and saving re-arms the timer immediately.
[diagnostics]
# Where config problems go, on top of always being written to the log.
#
# A typo used to vanish silently: an unknown key in a chord drops that bind, a
# malformed mode falls back to preferred, an unparseable color falls back to the
# default -- all of it logged and nowhere else. This decides who else hears
# about it.
#
#   "Osd"     desktop notification via notify-send (default -- a bar is not
#             guaranteed to exist, let alone to have room for a notification)
#   "Ipc"     a `config_error` message to every IPC subscriber, for a status
#             bar that wants to render it itself (see ipc.rs)
#   "Both"
#   "Silent"  log only -- the original behavior
#
# Startup problems are held back a few seconds before being reported, since the
# notification daemon is usually itself in `startup` and the bar has not
# connected yet.
config_errors = "Osd"

[idle]
# Master switch. Optional section: omitting [idle] entirely takes these
# defaults (a fresh install blanks its screen out of the box).
enabled = true
# Seconds of no activity before the screen powers off. 0 disables the timer
# regardless of `enabled`.
screen_off_seconds = 600

[layout]
# Number of columns instantiated on the monitor at startup.
# Mirrors Monitor::visible_columns -- config keys match the Rust identifiers exactly.
visible_columns = 3

# Gap, in pixels, between each column and the monitor edge / its neighbours.
# Monitor edges and inter-column seams both measure exactly outer_gap (the seam is
# split half-and-half between adjacent columns). Omitting the key defaults to 20.
outer_gap = 20

# Gap, in pixels, between tiled windows *within* a group's split tree. Exactly one
# inner_gap sits in each split seam. Omitting the key defaults to 10.
inner_gap = 10

# Per-connector output placement. Omitting this section (or omitting an entry
# for a given connector) is fine -- unconfigured outputs are auto-laid-out
# left-to-right so they never collide at the same origin. Uncomment and adjust
# to pin a specific connector's position (and optionally its mode).
# [[output]]
# name = "DP-3"            # connector name, as reported by the DRM backend
# position = [0, 0]        # top-left corner in global compositor space
# mode = "1920x1080"       # optional; omit to use the output's preferred mode
# primary = true           # optional, defaults to false
# hdr = true   # opt-in HDR (BT.2020/PQ) for this output; default false. Requires an HDR display.
#
# [[output]]
# name = "HDMI-A-1"
# position = [1920, 0]
# mode = "1280x400"
# transform = "270"  # normal|90|180|270|flipped|flipped-90|...|left|right; rotates the output

[animation]
# Duration of scroll/rotate slide tweens, in milliseconds. Omitting this
# section entirely is fine -- it defaults to 250.
duration_ms = 250

# Optional section: omitting [input] entirely is fine, taking the defaults below.
[input]
# Hovering a window gives it keyboard focus, instead of requiring a click.
# Live/hot-reloadable, and flippable at runtime with the ToggleFocusFollowsMouse
# keybind -- but a config save re-seeds the live value, so editing this is always
# the way back to a known state.
#
# Deliberately inert in three cases: while a pointer grab is in progress (a drag
# or resize owns the pointer), while any window is fullscreen (a stray hover
# would otherwise drop a game out of fullscreen), and over layer-shell surfaces
# such as the bar or rofi, which are not windows and must keep their keyboard.
focus_follows_mouse = false

# Window borders. Drawn OUTSIDE the client rect, over the surrounding gap, so a
# window's tile keeps its full size -- the trade is that borders eat into
# `inner_gap`/`outer_gap`. At `inner_gap = 0` adjacent borders overlap; at
# `outer_gap = 0` the outermost ring clips at the screen edge. Raise the gaps to
# give borders room.
#
# Fullscreen windows never get a border: an element stacked above a fullscreen
# window disqualifies it from direct primary-plane scanout, which is the path
# HDR gaming depends on.
[decoration]
# Logical pixels. 0 disables borders entirely and skips all decoration work.
border_width = 2

# Corner radius in logical pixels. 0 (the default) disables rounding entirely
# and keeps the batched render path the compositor used before rounding
# existed, so nobody pays for a feature they have not asked for.
#
# Rounding clips each window with a signed-distance mask in the fragment
# shader. Two consequences worth knowing:
#   - A rounded window is no longer opaque, so it stops contributing to
#     occlusion culling. Expect slightly more work per frame on a busy screen.
#   - Fullscreen windows are never rounded. There is no gap for a corner to
#     reveal, and rounding would cost the direct scanout that HDR gaming needs.
#
# The border follows this same radius, staying concentric with the window edge.
# corner_radius = 8

# Colors accept #RGB, #RRGGBB, or #RRGGBBAA (the leading '#' is optional). An
# alpha of 0 skips the border for that window without disabling the feature.
# Defaults are Catppuccin Mocha blue / surface1.
active_color = "#89b4fa"
inactive_color = "#45475a"

# Absolute border luminance in nits, applied ONLY on an output actually
# compositing in HDR (`hdr = true` on an HDR-capable display). Everywhere else
# -- SDR output, SDR hardware, HDR disabled, screenshots, screencasts -- it is
# ignored entirely and the border is simply `active_color`. Nobody without an
# HDR monitor is affected by anything in this block.
#
# The point is chrome brighter than SDR white: with sdr_white_nits at its 203
# default, a 350-nit focus ring is a focus indication that SDR chrome
# physically cannot express. Omit (or set 0) for "no opinion" -- the border
# then sits at SDR white like every other SDR element.
# Commented out so this file matches the built-in defaults exactly: a config
# omitting [decoration] entirely gets no luminance opinion. Uncomment to opt in.
# # active_luminance_nits = 350.0
# inactive_luminance_nits = 0

# Glow: how far the border's light spills past the ring, in logical pixels.
# 0 (the default) draws a plain ring and nothing else. The glow is drawn from
# the same rounded-rect distance field as the ring, so it stays concentric with
# the corner curve instead of pooling at the corners.
#
# This is where HDR earns its keep. In SDR a glow has to brighten toward white
# to be visible at all, which washes out the hue and competes with window
# content -- which is why glows tend to look gaudy. With a luminance above SDR
# white the glow keeps its saturation and simply sits brighter than the desktop,
# reading as light spilling off the window edge. Subtlety comes from the
# headroom, not from turning the opacity down.
#
# Keep the margin inside your gaps, or neighbouring windows will sit in each
# other's glow.
# active_glow_margin = 12
# inactive_glow_margin = 0

# Shapes how quickly the glow gives out: 1.0 is linear, higher keeps it tight
# to the border, lower spreads it across the gap. Applies to both states, and
# can be overridden per rule.
# glow_falloff = 2.0

# Window opacity, 0.0-1.0. 1.0 (the default) leaves windows untouched and keeps
# every optimization an opaque window gets -- below 1.0 a window stops
# contributing to occlusion culling, so things behind it must be drawn.
#
# Fullscreen windows are always forced opaque regardless of any rule: a
# translucent window cannot take direct primary-plane scanout, which is the path
# a fullscreen window exists to get.
#
# Note this composites in the linear working space on an HDR output, so
# translucency blends in light rather than in gamma-encoded values -- which is
# more correct than most compositors manage, and looks it.
# active_opacity = 1.0
# inactive_opacity = 0.92

# Opacity for a window that is covered by other windows. 1.0 (the default)
# switches the whole occlusion check off, including its per-frame geometry.
#
# This exists because of translucency, not in spite of it. Promote a window over
# its neighbours and, at anything under full opacity, those neighbours bleed
# through it and the result is muddy. Fading what is underneath means what shows
# through a promoted window is mostly wallpaper instead of a pile of other
# windows.
#
# Windows above count as occluders whatever their own opacity: if the window on
# top were opaque, the one below would already be culled and none of this would
# matter.
#
# Fading only ever goes one way -- a window a rule already made more transparent
# stays that way, and being covered can only fade it further.
# obscured_opacity = 0.25

# How covered a window must be, 0.0-1.0, before it counts as obscured. The 0.9
# default means a window peeking out by a few pixels behind a maximized
# neighbour still fades.
# obscured_threshold = 0.9

# Per-window rules, applied in file order over the defaults above.
#
# `app_id` and `title` are case-insensitive SUBSTRING matches. When both are
# given both must match; a rule with neither matches every window, which makes a
# useful catch-all. Matching is on the Wayland app_id or the X11 WM_CLASS.
#
# Rules ACCUMULATE rather than the first match winning, and each rule changes
# only the fields it names. So a broad rule followed by a narrow exception works
# the obvious way: dim everything, then pin one class or title back.
#
# [[decoration.rule]]
# app_id = "signal"
# active_color = "#f38ba8"
# active_luminance_nits = 600.0
#
# Exempt video from a global dim, by title:
#
# [[decoration.rule]]
# title = "youtube"
# active_opacity = 1.0
# inactive_opacity = 1.0
#
# [[decoration.rule]]
# app_id = "alacritty"
# active_color = "#89b4fa"
# inactive_color = "#45475a"
# active_opacity = 0.95
# inactive_opacity = 0.85

# Keybinds map a chord to a NavAction variant. The value is the *exact* Rust
# variant name -- serde deserializes it straight into the enum, so there is no
# translation table in code. Chord syntax: modifier tokens (Super/Alt/Ctrl/Shift,
# case-insensitive) joined with '+', ending in an xkb keysym name ("Return", "h").
# Alt-based defaults so the binds survive nested testing under a host WM that
# grabs Super; switch to Super for a real TTY session.
[keybinds]
"Alt+h" = "RotateColumnsLeft"
"Alt+l" = "RotateColumnsRight"
"Alt+j" = "ScrollColumnDown"
"Alt+k" = "ScrollColumnUp"
"Alt+Return" = "MoveToNewColumn"
"Alt+Shift+H" = "MoveActiveColumnLeft"
"Alt+Shift+L" = "MoveActiveColumnRight"
# Relocate the focused window across the group grid: h/l cross columns (landing in
# the destination column's active row), j/k walk rows within the column. j=Down /
# k=Up to match the ScrollColumn binds above.
"Alt+Control+h" = "MoveFocusedWindowLeft"
"Alt+Control+l" = "MoveFocusedWindowRight"
"Alt+Control+j" = "MoveFocusedWindowDown"
"Alt+Control+k" = "MoveFocusedWindowUp"
# Zoom the cube: more visible columns (narrower bands) / fewer (wider bands).
"Alt+Shift+J" = "IncrementVisibleColumns"
"Alt+Shift+K" = "DecrementVisibleColumns"
"Alt+n" = "NewGroup"
# Flip the focused window's parent split between horizontal and vertical.
"Alt+r" = "FlipSplitDirection"
"Alt+Shift+space" = { Spawn = "alacritty" }
# Launcher. rofi picks its wayland (layer-shell) backend automatically under
# Rubix; the compositor grants it keyboard focus via layer-shell keyboard
# interactivity (see focus_interactive_layer).
"Super+space" = { Spawn = "rofi -show drun" }
# Audio. Bare XF86 media keysyms (resolved case-sensitively by xkb) reach the
# compositor via libinput on the TTY; each Spawns a pactl command against the
# default sink/source. The waybar volume module reacts on its own via `pactl
# subscribe`, so no compositor-side sink handler is needed. pactl matches the
# bar's tooling (@DEFAULT_SINK@).
"XF86AudioRaiseVolume" = { Spawn = "pactl set-sink-volume @DEFAULT_SINK@ +5%" }
"XF86AudioLowerVolume" = { Spawn = "pactl set-sink-volume @DEFAULT_SINK@ -5%" }
"XF86AudioMute" = { Spawn = "pactl set-sink-mute @DEFAULT_SINK@ toggle" }
"XF86AudioMicMute" = { Spawn = "pactl set-source-mute @DEFAULT_SOURCE@ toggle" }
# SDR-white-nits "brightness slider" for HDR outputs (see sdr_white_nits
# above). Alt-based to match this file's nested-testing convention; switch to
# Super for a real TTY session (matches "Super+space" launcher above).
"Alt+XF86AudioRaiseVolume" = "IncreaseSdrWhite"
"Alt+XF86AudioLowerVolume" = "DecreaseSdrWhite"
# Live A/B toggle: flips HDR on/off on every HDR-capable output with no
# restart. Shifted-letter final token (matches Alt+Shift+H/+L/+J/+K above --
# the key event's keysym is already the shifted symbol when Shift is held).
"Alt+Shift+T" = "ToggleHdr"
# Flip focus-follows-mouse live, for A/B without editing the config (see [input]).
"Alt+Shift+F" = "ToggleFocusFollowsMouse"
# Exit the compositor. On the TTY backend this is the only in-session quit (winit
# also closes on the host window's close button). Ctrl+Alt+Fn switches VTs.
# Uppercase Q: with Shift held, xkb resolves the key to its shifted symbol, so the
# chord's final token must be the shifted form (matching Alt+Shift+H / +L above).
"Alt+Shift+Q" = "Quit"
